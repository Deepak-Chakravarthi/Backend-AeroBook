spring:
  profiles:
    active: dev

  # Dev-specific settings
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.aerobook: DEBUG
    com.aerobook.boundary: WARN  # shows boundary violations clearly